"use client";

/**
 * Re-export do tema modularizado
 * Para imports legados que usam '@/lib/theme'
 */
export { getTheme, darkTheme, lightTheme, colors, typography, breakpoints, responsiveHelpers } from './theme/index';
